{
  "name": "trabalho-final-pgats-automacao-api",
  "version": "1.0.0",
  "description": "API REST para gestão de prontuários de animais de estimação (fundação)",
  "main": "src/rest/server.js",
  "scripts": {
    "start": "npm run start:rest",
    "start:rest": "nodemon src/rest/server.js",
    "start:apollo": "node src/graphql/server.js",
    "test": "mocha tests/**/*.test.js --timeout 10000 --reporter mochawesome",
    "test:rest:external": "mocha tests/rest/external/**/*.test.js --timeout 10000 --reporter mochawesome",
    "test:rest:controller": "mocha tests/rest/controller/**/*.test.js --timeout 10000 --reporter mochawesome",
    "test:graphql:external": "mocha tests/graphql/external/**/*.test.js --timeout 10000 --reporter mochawesome",
    "test:graphql:controller": "mocha tests/graphql/controller/**/*.test.js --timeout 10000 --reporter mochawesome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server-express": "^3.11.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^4.6.3"
  },
  "devDependencies": {
    "chai": "^6.2.0",
    "mocha": "^11.3.0",
    "mochawesome": "^7.1.4",
    "nodemon": "^3.1.10",
    "sinon": "^21.0.0",
    "supertest": "^7.1.4"
  }
}
